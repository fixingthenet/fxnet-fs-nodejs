#!/bin/bash
export PATH=$PATH:/code/node_modules/.bin

mode=${1:-prod}

if [ $mode == "dev" ]; then
  if [ ! -d /code/node_modules ]; then
    yarn install
  fi
  exec yarn run dev
else
  exec yarn run start
fi  

